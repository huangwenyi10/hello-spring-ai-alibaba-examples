server:
  port: 8888

spring:
  application:
    name: mcp-webflux-client-example
  main:
    # 把 web-application-type 设置成 none，代表这是一个非 Web 控制台应用
    web-application-type: none
  ai:
    dashscope:
      api-key: ${AI_DASHSCOPE_API_KEY}
    mcp:
      client:
        sse:
          connections:
            server1:
              # 实际的连接地址为：http://localhost:8080/sse/mcp
              url: http://localhost:8080/
